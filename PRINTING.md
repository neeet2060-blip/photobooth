# 인화(프린트) 부가 기능

QR 다운로드는 여전히 **무료**이며 이 기능과 무관하게 그대로 동작합니다.
아래는 새로 추가된 **유료 인화** 부가 기능에 대한 설명입니다.

## 전체 흐름

1. 방문자가 필터 화면에서 "완성"을 누르면, 기존과 동일하게 `final.jpg`가
   `/api/final`로 업로드되고 서버가 `filter → qr` 전환을 수행합니다
   (이 부분은 변경되지 않았습니다).
2. 같은 타이밍에 클라이언트가 **백그라운드로** 인쇄용 시트(`print.jpg`,
   1200x1800px, 300dpi 4x6인치 규격)를 만들어 `/api/print-sheet`로
   업로드합니다. 이 업로드가 실패해도 방문자 경험(QR 다운로드)에는
   전혀 영향을 주지 않습니다 — 콘솔에 에러만 기록됩니다.
3. QR 화면에서 방문자(또는 직원)가 "사진 인화 주문하기"를 눌러 매수를
   고르고 수량을 확정하면, 화면은 "카운터에서 결제해주세요" 상태로
   바뀝니다.
4. **직원이 물리적인 SumUp 카드 단말기로 결제를 진행**하고, 결제가
   완료되면 부스 화면에서 "결제 확인" 버튼을 누릅니다.
5. 이 버튼 클릭이 `confirmPrintPayment` 액션이 되어 서버가 인쇄
   작업(print job)을 큐에 등록합니다.

## 결제 모델: 왜 앱이 카드 데이터를 절대 다루지 않는가

이 앱은 SumUp이나 어떤 결제 게이트웨이와도 통신하지 않습니다. 카드
정보는 전적으로 물리적인 SumUp 단말기와 카드 사이에서만 오가고, 앱은
"직원이 결제가 완료됐다고 확인했다"는 사실만 기록합니다.

이 확인은 `server/state.js`의 `confirmPrintPayment` 액션 **단 하나의
지점(seam)**으로 설계되어 있습니다. 이 액션이 하는 일은:

- `state.printOrder`가 `awaiting_payment` 단계일 때만 허용
- `{ type: 'print-order-confirmed', sessionId, quantity, unitPriceCents,
  totalCents }` 이펙트를 하나 발행
- `state.printOrder`를 `null`로 초기화 (다시 주문 가능한 상태로 복귀)

나중에 실제 결제 게이트웨이(SumUp API, Stripe Terminal 등)를 연동하고
싶다면, 이 액션을 호출하는 지점만 "게이트웨이가 결제 성공을 콜백으로
알려줬을 때"로 바꾸면 됩니다. 상태 머신, 인쇄 큐, 정산 로직은 전혀
건드릴 필요가 없습니다.

## folder 모드 vs cups 모드

`/admin`의 "인화 설정" 탭에서 `printMode`를 전환합니다.

- **folder (기본값, 안전)**: 실제 프린터가 연결되어 있지 않아도 항상
  동작합니다. 인쇄 작업이 실행되면 `data/print-outbox/` 폴더에
  `{timestamp}-{jobId}-x{매수}.jpg` 형태로 파일이 복사됩니다. 행사
  담당자가 이 폴더를 감시하는 별도 프린트 프로그램(또는 수동 인쇄)으로
  실제 인쇄를 진행합니다.
- **cups (Linux/macOS의 CUPS 인쇄 시스템 연동)**: `printerName`,
  `printMedia`를 설정하면 `lp -d <printerName> -n <매수> -o
  media=<printMedia> -o fit-to-page <파일>` 커맨드를 실행합니다.
  프린터 이름/용지 크기는 `^[a-zA-Z0-9_.-]{1,64}$` /
  `^[a-zA-Z0-9x]{1,16}$` 화이트리스트로 검증한 뒤에만 사용되며, 항상
  argv 배열로 실행되어(셸 문자열 조합 없음) 셸 인젝션이 불가능합니다.

## 인쇄 작업 vs 세션 삭제 경쟁 조건, 그리고 해결책

부스는 QR 화면에서 일정 시간(`qrTimeoutSec`)이 지나면 자동으로
`restart`, 또는 유휴 상태에서 `forceReset`이 발생해 세션 사진 폴더
(`data/photos/<sessionId>/`, `print.jpg` 포함)를 삭제합니다.

문제: 방문자가 QR 화면에서 인화를 주문하고 결제 확인까지 마친 직후,
곧바로 타임아웃 스윕이 실행되면 `print.jpg`가 인쇄 큐 워커가 실제로
파일을 읽기도 전에 삭제될 수 있습니다.

**해결책**: `server/printqueue.js`의 `enqueueJob()`은 인쇄 작업을 큐에
등록하기 **전에** `print.jpg`를 `data/print-jobs/<jobId>.jpg`라는 별도
스테이징 경로로 즉시(eagerly) 복사합니다. `server/index.js`가
`print-order-confirmed` 이펙트를 처리하며 `enqueueJob`을 호출하고, 그
호출(과 그 안의 `await copyFile`)이 완료된 뒤에야 `dispatch()`가
반환됩니다. 따라서 세션 폴더가 그 이후에 삭제되더라도 인쇄 작업은 이미
독립적인 자기 파일 사본을 갖고 있어 전혀 영향을 받지 않습니다.

## 매출 정산 정책: 왜 취소된 것만 매출에서 제외하는가

결제는 인쇄 작업이 생성되기 **이전에** 이미 직원에 의해 오프라인으로
확인되었습니다. 즉, 인쇄 작업(printjobs.json의 레코드)이 존재한다는
사실 자체가 "결제가 있었다"는 증거입니다.

- 인쇄가 실패(`failed`)하더라도 이는 프린터 고장 등 **운영상의 문제**일
  뿐, 결제가 취소된 것이 아니므로 매출에 포함됩니다.
- 오직 `canceled` 상태(=`queued` 상태에서만 취소 가능하므로, 실제로
  한 번도 인쇄가 시도된 적 없는 작업)만 매출 집계에서 제외됩니다.

`GET /api/admin/printsettlement`는 정수 센트 단위로만 계산하고,
EUR 문자열 포맷팅은 `public/admin/admin.js`의 표시 단계에서만
수행합니다.

## 프린터 선정 후 조정할 항목

- 용지 여백/커팅 보정: 실제 프린터·용지 조합에 맞춰 `fit-to-page` 옵션과
  `composePrintSheet`의 여백/커팅선 위치를 미세 조정해야 할 수 있습니다.
- 드라이버 옵션(무광/유광, 자동 커터): CUPS 드라이버가 지원하는 추가
  `-o` 옵션(광택 마감, 자동 커터 등)을 `printer.js`의 `extraOptions`
  경로에 연결해 확장하세요.
- 색상 보정: 화면(sRGB)과 실제 인화 결과물의 색감 차이를 프린터
  드라이버의 ICC 프로파일/색보정 설정으로 맞춰야 합니다.
- 물리적 정렬 테스트: 스트립 레이아웃의 커팅선이 실제 프린터 출력에서도
  정확히 절반 지점에 오는지, 그리드 레이아웃의 캡션이 잘리지 않는지
  실물 인쇄로 반드시 확인하세요.
