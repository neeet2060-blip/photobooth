# 클라우드 배송(사진 다운로드) 부가 기능

QR 다운로드는 기본적으로 **LAN 전용**입니다: `/api/final`이 만드는 링크는
`http://<노트북의 LAN IP>:<HTTP 포트>/p/<token>` 형태이고, 방문자의 폰이
행사장 핫스팟/공유기에서 벗어나는 순간(귀가 후, 모바일 데이터로 전환한
순간) 더 이상 접속되지 않습니다.

이 문서는 그 한계를 없애기 위한 **선택적** 부가 기능인 클라우드 배송을
설명합니다. 자격 증명이 하나도 없어도 부스는 지금처럼 완벽하게 동작하며,
이 기능은 순수하게 "있으면 더 좋은" 레이어입니다.

## 왜 필요한가

- 행사장 핫스팟은 인터넷에 연결되어 있지 않거나, 연결되어 있어도 방문자가
  자리를 뜨면 더 이상 그 네트워크에 있지 않습니다.
- 방문자가 나중에 집에서, 또는 이동 중 모바일 데이터로 QR을 다시 열어보고
  싶을 수 있습니다.
- 클라우드 배송이 활성화되어 있으면 QR이 가리키는 링크가 `https://` 클라우드
  URL이 되어, 어디서든 계속 동작합니다.

## 활성화 없이도 항상 동작합니다

`secrets/firebase-admin.json`이 없거나, `FIREBASE_STORAGE_BUCKET` 환경
변수가 설정되어 있지 않으면 `server/cloud.js`의 `isCloudEnabled()`는 그냥
`false`를 반환하고, `/api/final`은 지금까지와 완전히 동일하게 LAN URL만
사용합니다. 서버 부팅은 이 두 값이 하나도 없어도 절대 실패하지 않습니다.

## 활성화 방법

1. **Firebase 프로젝트 확인**: 이미 다른 앱이 쓰고 있는 Firebase
   프로젝트(예: TOK2026의 `taste-of-korea-3ac1b`)를 재사용하거나, 새
   프로젝트를 만들 수 있습니다. 이 부가 기능은 **버킷을 공유**해도
   안전하도록 설계되어 있습니다 (아래 "prefix 격리" 참고).
2. **서비스 계정 키 발급**: Firebase 콘솔 → 프로젝트 설정 → 서비스 계정 →
   "새 비공개 키 생성"으로 JSON 파일을 하나 받습니다.
3. **키 배치**: 그 JSON 파일을 이 저장소의 `secrets/firebase-admin.json`
   경로에 둡니다. 다른 경로를 쓰고 싶다면 `FIREBASE_CREDENTIALS` 환경
   변수로 전체 경로를 지정하세요. `secrets/`는 `.gitignore`에 포함되어
   있어 실수로 커밋되지 않습니다 — 그래도 **절대 이 파일을 git에
   추가하거나, 로그/커밋 메시지/스크린샷 등 어디에도 내용을 노출하지
   마세요.**
4. **버킷 이름 지정**: 두 가지 방법이 있습니다.
   - **권장: `/admin` → 설정 → "Firebase Storage 버킷"** 칸에 버킷 이름
     (예: `my-project.appspot.com`)을 입력하고 저장합니다. `data/settings.json`에
     저장되어 서버를 몇 번을 재시작해도 유지됩니다. 행사 당일 노트북에서
     서버를 켤 때마다 환경 변수를 다시 설정해줄 필요가 없어 훨씬 안전합니다.
   - 또는 `FIREBASE_STORAGE_BUCKET` 환경 변수로도 설정할 수 있습니다
     (설정돼 있으면 이 값이 항상 관리자 설정보다 우선합니다). 이건 매번
     서버를 실행하는 셸에 다시 export해야 해서, 로컬 개발/테스트용으로만
     권장합니다.
   - 버킷 이름이 없으면(둘 다 비어있으면) 자격 증명 파일이 있어도 클라우드는
     비활성 상태로 취급됩니다.
5. 관리자 설정으로 버킷을 지정했다면 저장 즉시(다음 방문자의 `/api/final`
   요청부터) 반영됩니다 — 서버 재시작이 필요 없습니다. 환경 변수로
   설정했다면 `npm start`로 서버를 다시 시작해야 반영됩니다.

```bash
# 로컬 개발/테스트용 예시 — 실제 행사에서는 /admin → 설정에서 버킷을
# 지정하는 쪽을 권장합니다 (위 참고).
export FIREBASE_STORAGE_BUCKET="my-project.appspot.com"
# FIREBASE_CREDENTIALS는 기본 경로(secrets/firebase-admin.json)를 쓸 거라면 생략 가능
npm start
```

## `photobooth/` prefix 격리, 그리고 왜 그 앱의 보안 규칙을 절대 건드리지 않는가

이 앱은 **Firebase Admin SDK**로 클라우드에 접근합니다. Admin SDK는
서비스 계정 자격 증명으로 동작하며, **Firestore/Storage 보안 규칙을
완전히 우회**합니다 — 이것이 서비스 계정을 쓰는 이유 그 자체입니다: 이
앱은 다른 앱의 보안 규칙을 전혀 몰라도, 읽거나 쓸 필요도 없이 올바르게
동작할 수 있습니다.

바꿔 말하면, 이 앱이 그 버킷 안에서 "자기 영역만" 건드리는 것은 **보안
규칙이 아니라 애플리케이션 코드의 규율(discipline)**로 보장됩니다:

- `server/cloud.js`의 모든 업로드/삭제는 `photobooth/<token>/final.jpg`
  형태의 경로만 만듭니다. `token`은 앱 전체에서 쓰는 24자리 hex 정규식
  (`/^[a-f0-9]{24}$/`)으로 검증된 값만 허용되고, 검증에 실패하면 즉시
  에러를 던집니다.
- 이 모듈은 `photobooth/` prefix 바깥의 어떤 객체도 나열(list)하거나
  읽거나 삭제하지 않습니다.

그래서 이 프로젝트를 위해 다른 앱의 Storage/Firestore 보안 규칙을
수정하거나 배포할 필요가 **전혀 없습니다** — 그렇게 하면 오히려 그
앱의 보안 경계를 약화시킬 위험만 생깁니다. 이 저장소의 코드는 그 규칙을
절대 배포하지 않으며, 앞으로도 그래서는 안 됩니다.

## 업로드 실패/타임아웃 시 동작: 기존과 동일한 LAN URL 폴백

`/api/final`은 클라우드가 활성화되어 있으면 업로드를 시도하지만, 방문자의
QR 화면이 느려지거나 멈추는 일이 없도록 **하드 타임아웃**을 겁니다
(`관리자 → 설정`의 "클라우드 업로드 제한시간(ms)", 기본 8000ms).

- 업로드가 제한시간 안에 성공하면 → QR/응답의 `finalUrl`은 클라우드
  `https://` URL이 되고, 토큰 레코드에 `cloudUrl`/`uploadedAt`이 저장됩니다.
- 타임아웃되거나 업로드가 실패하면 → `console.warn`으로 짧게 기록만 하고,
  **오늘까지의 동작과 완전히 동일한 LAN URL**로 조용히 폴백합니다. 방문자
  경험(QR 화면 표시 자체)은 전혀 막히지 않습니다.

타임아웃에서 진 업로드 프라미스는 나중에 뒤늦게 resolve/reject되어도
무시되도록 처리되어 있어, unhandled rejection 경고가 나지 않습니다.

## 보관 정책(retention): autoDeleteHours가 클라우드 사본도 지웁니다

`관리자 → 설정`의 `autoDeleteHours`가 지나면 LAN 쪽 사진 폴더뿐 아니라
클라우드 사본도 함께 삭제됩니다. `server/index.js`가
`AUTO_DELETE_SWEEP_INTERVAL_MS`(10분)마다 `server/routes.js`의
`sweepExpiredTokens()`를 호출해서:

1. `cloudUrl`이 있는 토큰 중 `createdAt`이 `autoDeleteHours`보다 오래된
   것을 찾고,
2. `cloud.deleteCloudObject(token)`으로 `photobooth/<token>/final.jpg`를
   지운 뒤,
3. 성공하면 그 토큰을 `tokens.json`에서도 지웁니다.

삭제가 실패하면(네트워크 문제 등) 그 토큰은 그대로 남겨두고
`console.warn`만 남기며, 다음 10분 뒤 스윕에서 다시 시도합니다 — 절대
스윕/서버가 죽지 않습니다.

## 동작 확인 방법

1. 위 "활성화 방법"대로 `secrets/firebase-admin.json`과
   `FIREBASE_STORAGE_BUCKET`을 설정하고 서버를 재시작합니다.
2. 실제 세션을 하나 끝까지 진행합니다 (테마 선택 → 촬영 → 선택 → 필터 →
   완성).
3. QR 화면에서 QR코드를 스캔하기 전에, 코드 내용이 `https://`로
   시작하는 클라우드 URL인지 확인하세요 (LAN IP가 아니어야 합니다).
   급하면 `/admin` → 통계처럼 `/api/final` 응답의 `finalUrl`을 확인해도
   됩니다.
4. `/admin` → **클라우드** 탭에서 "사용 중" 상태, 버킷 이름, "클라우드로
   전달된 사진" 카운트가 1 이상 올라갔는지 확인합니다.
5. (선택) 자격 증명 파일을 다시 지우거나, `/admin` → 설정에서 버킷 이름을
   비우고 저장해서, 부스가 여전히 완벽하게 동작하고 QR이 다시 LAN URL로
   돌아가는지 확인하세요 — 이것이 이 기능의 핵심 안전장치입니다.
